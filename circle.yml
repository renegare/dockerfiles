general:
    branches:
        only:
            - master

machine:
    services:
        - docker

test:
    override:
        - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
        - make build tag push IMAGE=nginx
        - make build tag push IMAGE=php
        - make build tag push IMAGE=phantomjs
        - make build tag push IMAGE=nprn-stack
